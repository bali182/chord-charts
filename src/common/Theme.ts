export type SectionTheme = {
  barHeight: number
  spacing: number
  radius: number
  strokeWidth: number
  opacity: number,
  colors: string[]
}

export type Theme = {
  width: number
  height: number
  spacing: number
  section: SectionTheme
}
